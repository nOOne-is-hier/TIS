#include <iostream>
// #include <windows.h>

using namespace std;

int main()
{
    // SetConsoleOutputCP(CP_UTF8);
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, M;
    cin >> N >> M;

    std::cout << "N: " << N << ", M: " << M << "\n";

    return 0;
}
